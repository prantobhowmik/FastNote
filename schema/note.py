def note(item) -> dict:
    return {
        "_id": str(item["id"]),
        "note": item["note"],
    }

def note(item) -> list:
    return {
        "_id": str(item["id"]),
        "note": item["note"],
    }